# src/main/resources/application-prod.yml
spring:
  application:
    name: ph-shoes-etl


  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties: { }

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: ${PORT:8080}

logging:
  level:
    root: INFO
